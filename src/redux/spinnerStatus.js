// // selectors
// export const getSpinnerStatus = ({ spinnerStatus }) => spinnerStatus;

// // actions
// const createActionName = (action) => {
//   return `app/spinnerStatus/${action}`;
// };

// const CHANGE_SPINNER_STATUS = createActionName('CHANGE_SPINNER_STATUS');

// // action creators
// export const changeSpinnerStatus = (payload) => ({
//   type: CHANGE_SPINNER_STATUS,
//   payload,
// });

// // reducer
// export const spinnerStatusReducer = (statePart = [], action) => {
//   switch (action.type) {
//     case CHANGE_SPINNER_STATUS:
//       return action.payload;
//     default:
//       return statePart;
//   }
// };
